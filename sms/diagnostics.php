<?php require __DIR__.'/lib/bootstrap.php';need_login();require __DIR__.'/lib/header.php';top('Diagnostics','Diagnostics');$checks=['PHP version'=>PHP_VERSION,'cURL'=>function_exists('curl_init')?'OK':'MISSING','JSON'=>function_exists('json_encode')?'OK':'MISSING','Sessions'=>session_status()===PHP_SESSION_ACTIVE?'OK':'NOT ACTIVE','Data directory'=>is_writable($DATA)?'Writable':'Not writable','WhereverSIM token'=>$CONFIG['token']?'Configured (raw Authorization header)':'Missing','Default ICCID'=>$CONFIG['iccid']?:'Not set'];?><div class="panel"><table><?php foreach($checks as $a=>$b):?><tr><th><?=htmlspecialchars($a)?></th><td><?=htmlspecialchars($b)?></td></tr><?php endforeach?></table></div><?php bottom();?>